<p><label>
  <%= title %>
  <select name = "<%= fieldname %>">
    <% collection.each do |item| %>
      <option value="<%= item.id %>"><%= item.send(fieldname) %></option>
    <% end %>
  </select>
</label></p>
