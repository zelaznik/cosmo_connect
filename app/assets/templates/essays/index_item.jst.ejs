<div>
  <div class="row response-index-item-title">
    <%= essay.escape('title') %>
  </div>
  <div class="row">
    <div class="col-xs-10 response-index-item-body">
      <%= essay.escape('body') || '(none)' %>
    </div>

    <% if (user.isCurrentUser) { %>
      <%= '<div class="col-xs-2" valign="bottom">' %>
        <%= '<button class="essay-edit btn btn-xs btn-danger pull-right">edit</button>' %>
      <%= '</div>' %>
    <% } %>
  </div>

</div>
