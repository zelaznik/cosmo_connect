options = {
  year: {
    min: 1900,
    max: 2015,
    value: 1985
  },

  month: {
    min: 1,
    max: 12,
    value: 7
  },

  day: {
    min: 1,
    max: 31,
    value: 14
  }
}

<% [year, month, day].forEach(function(place) { %>
  <selector name="birthdate[<%=  %>]"
<% }) %>

<selector name="birthdate[year]">
<% if (year) { %>
  <option disabled="true">Year</option>
<% } else { %>
  <option disabled="true" selected=true>Year</option>
<% } %>

</selector>
